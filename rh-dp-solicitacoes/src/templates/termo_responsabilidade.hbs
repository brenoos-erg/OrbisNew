<!doctype html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <style>
      @page { size: A4; margin: 14mm; }
      * { box-sizing: border-box; }
      body { margin: 0; font-family: 'Segoe UI', Arial, sans-serif; color: #1f2937; font-size: 12px; line-height: 1.45; }
      .page { min-height: calc(297mm - 28mm); border: 1px solid #d1d5db; padding: 16px 18px; display: flex; flex-direction: column; }
      .header { display: flex; align-items: center; justify-content: space-between; border-bottom: 2px solid #ea580c; padding-bottom: 8px; }
      .logo { max-width: 160px; max-height: 42px; object-fit: contain; }
      .company { text-align: right; font-size: 10px; color: #6b7280; }
      .title { margin: 16px 0 10px; text-align: center; font-size: 19px; letter-spacing: 0.2px; color: #111827; font-weight: 700; }
      .subtitle { text-align: center; margin-bottom: 14px; font-size: 11px; color: #4b5563; }
      .box { border: 1px solid #d1d5db; border-radius: 6px; margin-bottom: 10px; overflow: hidden; }
      .box h3 { margin: 0; padding: 7px 10px; background: #f8fafc; border-bottom: 1px solid #d1d5db; font-size: 11px; text-transform: uppercase; }
      .content { padding: 10px; }
      .grid { display: grid; grid-template-columns: 1fr 1fr; gap: 8px 14px; }
      p { margin: 0; }
      table { width: 100%; border-collapse: collapse; }
      th, td { border: 1px solid #cbd5e1; padding: 7px; font-size: 11px; vertical-align: top; }
      th { background: #f8fafc; text-align: left; font-weight: 600; }
      ul { margin: 0; padding-left: 18px; }
      li { margin-bottom: 4px; }
      .obs-row { display: flex; align-items: center; gap: 8px; margin-bottom: 6px; }
      .check { width: 11px; height: 11px; border: 1px solid #111827; display: inline-block; margin-left: 6px; }
      .signatures { margin-top: 22px; display: grid; grid-template-columns: 1fr 1fr; gap: 24px; }
      .signature-block { text-align: center; }
      .line { border-top: 1px solid #111827; margin-top: 44px; padding-top: 6px; font-size: 11px; }
      .footer { margin-top: auto; padding-top: 12px; border-top: 1px solid #e5e7eb; text-align: center; color: #6b7280; font-size: 10px; }
    </style>
  </head>
  <body>
    <main class="page">
      <header class="header">
        <div>{{#if logoDataUri}}<img src="{{logoDataUri}}" alt="ERG" class="logo" />{{/if}}</div>
        <div class="company">
          ERG Engenharia<br />
          Termo digital de responsabilidade
        </div>
      </header>

      <h1 class="title">TERMO DE RESPONSABILIDADE</h1>
      <p class="subtitle">Protocolo {{protocolo}} • Emitido em {{dataHora}}</p>

      <section class="box">
        <h3>Dados do solicitante</h3>
        <div class="content grid">
          <p><strong>Nome:</strong> {{nomeSolicitante}}</p>
          <p><strong>E-mail:</strong> {{email}}</p>
          <p><strong>Login:</strong> {{login}}</p>
          <p><strong>Telefone:</strong> {{telefone}}</p>
          <p><strong>Centro de custo:</strong> {{centroCusto}}</p>
        </div>
      </section>

      <section class="box">
        <h3>Equipamento alocado</h3>
        <div class="content">
          <table>
            <thead>
              <tr>
                <th style="width: 22%">Item</th>
                <th style="width: 43%">Modelo / Especificação</th>
                <th style="width: 20%">Patrimônio</th>
                <th style="width: 15%">Linha</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <td>{{equipamentoNome}}</td>
                <td>{{equipamentoModelo}}</td>
                <td>{{patrimonio}}</td>
                <td>-</td>
              </tr>
            </tbody>
          </table>
        </div>
      </section>

      <section class="box">
        <h3>Condições de uso</h3>
        <div class="content">
          <ul>
            {{#each regras}}<li>{{this}}</li>{{/each}}
          </ul>
          <p><strong>{{aceite}}</strong></p>
        </div>
      </section>

      <section class="box">
        <h3>Observações de vistoria</h3>
        <div class="content">
          <div class="obs-row">Tela riscada? <span class="check"></span> Sim <span class="check"></span> Não</div>
          <div class="obs-row">Avaria aparente? <span class="check"></span> Sim <span class="check"></span> Não</div>
          <div class="obs-row">Carregador original entregue? <span class="check"></span> Sim <span class="check"></span> Não</div>
        </div>
      </section>

      <section class="signatures">
        <div class="signature-block">
          <div class="line">Assinatura do colaborador</div>
        </div>
        <div class="signature-block">
          <div class="line">Responsável de TI</div>
        </div>
      </section>

      <footer class="footer">
        Documento emitido eletronicamente pelo RH-DP Solicitações • ERG Engenharia
      </footer>
    </main>
  </body>
</html>