{
  "name": "rh-dp-solicitacoes",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev.js",
    "prebuild": "prisma generate --schema ./prisma/schema.prisma",
    "build": "next build --webpack",
    "build:turbo": "next build",
    "start": "next start",
    "lint": "tsc --noEmit",
    "test": "npm run test:workflow-notifications && node tests/rq063-cost-center-submission.test.cjs && node tests/rq063-rh-fields-initial-submission.test.cjs && npm run test:regression",
    "prisma:generate": "prisma generate --schema ./prisma/schema.prisma",
    "prisma:migrate": "prisma migrate dev --schema ./prisma/schema.prisma",
    "seed": "prisma db push --schema ./prisma/schema.prisma && ts-node -P tsconfig.seed.json -r tsconfig-paths/register prisma/seed.ts",
    "generate